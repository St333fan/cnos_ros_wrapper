defaults:
  - user: default
  - machine: local
  - callback: base 
  - data: bop
  - model: cnos_fast # Use cnos_fast instead of cnos, saves 2GB VRAM
  - _self_

save_dir: ${machine.root_dir}/results/${name_exp}
name_exp: cnos_exps
dataset_name:
